"""Audiobookshelf ontology contribution (CONCEPT:AU-KG.ontology.federation-provider-leg).

Data-only subpackage: it carries ``audiobookshelf.ttl`` (the ``owl:Ontology``
``http://knuckles.team/kg/audiobookshelf`` module) which the Agent Utilities hub
federates through the ``agent_utilities.ontology_providers`` entry point. It contains
no live record, endpoint, credential, instance mapping, signature, or graph writer.
"""
